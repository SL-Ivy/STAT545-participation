---
title: "Tibble Exploration"
author: "SL_Ivy"
output: ioslides_presentation
---

```{r load, warning = FALSE, echo = FALSE}
# things inside the ```{r}``` are executable, the chunk options can be set to fit different purposes, such as echo, warning. More chunk options can be found: /https://rmarkdown.rstudio.com/lesson-3.html
library(tibble)
library(gapminder)
library(DT)
```


## Print out gapminder data:

```{r}
x <- gapminder
```

<!--assign the x a value which equal to gapminder-->

## Convert mtcars to a tibble, and wrap in datatable:

```{r}
datatable(as_tibble(mtcars))
```